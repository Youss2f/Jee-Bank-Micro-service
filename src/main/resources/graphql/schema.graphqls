type Query {
    accountsList: [Compte]!
    accountById(id: String!): Compte
}

type Compte {
    id: String!
    createdAt: String
    balance: Float
    currency: String
    type: AccountType
}

enum AccountType {
    CURRENT_ACCOUNT
    SAVING_ACCOUNT
}
